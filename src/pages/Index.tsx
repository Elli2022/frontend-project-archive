import Home from "./Home";

/**
 * Root route. The actual home page lives in `Home.tsx`.
 * Keeping this thin file so the project layout matches the rest of the routes.
 */
const Index = () => <Home />;
export default Index;
